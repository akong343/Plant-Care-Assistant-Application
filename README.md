# Plant-Care-Assistant-Application

## Overview
A Plant Care Assistant application, intended to function as a personal companion for plant owners.

## How to Run and Use the Program
1. Clone the repo
2. Install dependencies: `pip install -r requirements.txt`
3. Run the application with: `python app.py`
